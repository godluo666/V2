# 《月汐町》当前场景需求基线

> 状态：已确认  
> 基线日期：2026-07-31
> 当前实现分支：`agent/sunny-town`

这份文件记录已经确认的场景方向和不可回退项。后续增加场馆、玩法、家具或活动时，
先在“增量需求记录”追加条目，再修改共享布局、客户端表现、服务端规则和云端验收。

## 1. 不可回退的产品约束

- 保持现有团子角色的模型、配色能力、动画、控制手感和联机表现不变。
- 所有运行依赖、脚本、资源声明和测试依赖只能位于当前仓库内；不依赖全局安装或仓库外文件。
- 不在本地运行测试、构建或浏览器验收。验证统一交给 GitHub Actions 云端工作区。
- 当前首阶段街区只保留三个可进入场馆：
  - 星汐电影院
  - 团子轰趴馆
  - 镜界电竞观战馆
- 旧咖啡馆、便利店、游戏厅、团子塔和车站不出现在当前街道，也不提供入口。

## 2. 一番街户外基线

- 街道参考 P5 涩谷街头的“高对比、斜向构图、密集招牌、漫画都市节奏”，但不复制其
  商标、角色、字体、地图、贴图或具体美术资产；最终视觉必须是《月汐町》的原创表达。
- 画面要绚烂、明媚、有阳光，同时保持角色、入口、路面和交互提示清晰可读。
- 使用 `58m × 46m` 可玩边界；`7.2m` 宽的东西主街和同宽南北短街组成紧凑十字路口。
  路口图形区仅 `10m × 7.2m`，不再用大型十字广场、长支路或空旷车站区域制造距离。
- 四个街角从路缘起即由 `22–42m` 的近景高楼、场馆和住宅立面围合，中间只保留车辆与
  行人通行所需面积；不允许建筑后退形成无内容的大块空地。
- 三个场馆嵌在连续高层立面里，住宅、公寓、阳台、雨棚、海报、电线、自行车和花槽
  共同形成“紧凑的一条街”。
- 路面使用深墨沥青作为构图底色，以红、青、黄的小面积图形、招牌和灯带提亮。
- 入口前、出生点和主步行带保持净空；装饰物靠步道边缘成组布置。
- 户外服务端碰撞、客户端渲染和云端浏览器路径的唯一坐标来源是
  `shared/src/cityplan.ts`，不得另写一套散落坐标。

## 3. 三个场馆基线

### 星汐电影院

- 保留巨幕与 50 个座位，座区采用 5 排、三段分组和宽过道。
- 椅脚必须落在地面或对应排的台阶顶面；台阶高度、椅子原点、坐下吸附点和角色高度
  使用同一份共享布局数据，禁止仅靠视觉偏移修补浮空。
- 视频、YouTube 和控制栏必须显示同一个房间权威播放状态。

### 团子轰趴馆

- 氛围参考温馨社团活动室：奶油墙面、暖木地板、软毯、围坐沙发、茶水零食台、
  阅读角、社团活动板、照片奖杯墙、小舞台和点歌机。
- 保留可玩的服务端权威象棋桌。
- 放置一张有四方座位和棋子的飞行棋桌。首阶段它是可围坐的实体桌游陈设；
  若以后增加在线规则，必须走服务端权威状态，不能伪装成已联机玩法。
- 家具沿墙或成组放置，中部和入口保留通行空间，整体温馨但不拥挤。

### 镜界电竞观战馆

- 保留中央联赛大屏、观战区、机位和清晰主通道。
- 使用蓝青强调光配合暖色前台，避免压抑的纯黑网吧效果。
- 大屏共享播放状态与电影院使用同一套媒体同步基础设施。

## 4. 媒体同步基线

- 参考 VideoTogether 的同步思想：房间状态携带播放时间、暂停状态、播放速度和更新时间；
  客户端通过请求往返时间中点估计服务器时钟偏移。
- 服务端是媒体状态、修订号和房间广播的唯一权威来源。
- 所有玩家的进度条根据“服务器时间 + 权威媒体锚点”计算，不读取各自视频元素的本地
  `currentTime` 作为 UI 真值。
- 客户端忽略旧修订，断线重连和晚加入时使用最新快照恢复。
- 播放中的漂移策略：
  - `≤ 150ms`：保持正常速率；
  - `150–500ms`：支持细粒度速率的平台用约 `±3%` 的短时微调；
  - `> 500ms`：跳转到权威时间；
  - 暂停状态出现超过 `150ms` 的偏差时直接对齐。
- 缓冲期间不反复跳转；恢复可播放后重新对齐。
- 任意跨域网页 iframe 内的视频无法由宿主页可靠控制，不得虚假宣称同步。一起看内容
  应使用官方 YouTube 适配器或直接视频地址；新增提供方时实现统一 Provider Adapter。

## 5. 本轮完成后的维护性审计

### 已在第二轮优化中收口

- **同步策略重复**：直接视频和 YouTube 不再各自维护一套漂移阈值，统一调用
  `client/src/world3d/media/provider.ts` 的纯函数校正计划；新增 Provider 必须复用它。
- **首次进入可能延迟对齐**：播放器首挂载允许立即跳到权威时间，只有后续连续跳转受冷却限制。
- **YouTube 地址形态单一**：统一解析 `watch`、`youtu.be`、`shorts`、`live` 和
  `youtube-nocookie/embed`，解析规则有独立云端单测。
- **场馆入口信息重复**：入口名称、空间键和坐标统一从 `VENUES` 生成，增加场馆时不再
  同时维护第二张映射表。
- **当前城市被旧设施常量污染**：车站和天桥坐标已从 `shared/src/cityplan.ts` 移除；
  尚未删除的旧程序化预制件只保留本地归档尺寸，不再参与共享城市契约。
- **模块初始化隐式修改数组**：远景楼群改为纯函数生成后一次性合并，避免导入模块时
  对导出数组执行 `push`。
- **无入口旧房间的返回坐标失效**：兼容保留的旧房间统一回到一番街安全出生点，
  避免历史会话或内部跳转回到新边界之外。
- **浏览器旅程看见提示却不互动**：门和银幕交互统一走
  `scripts/browser-driver.mjs`；每次移动后必须重新检查提示、按下真实的 `E`，并等待目标
  空间确认，避免最后一次移动后漏检造成十几分钟的级联假失败。
- **云端低帧率让按键行走超时**：云端浏览器不再把 Three.js 渲染帧率当作移动时钟；
  旅程从最新服务端自身快照出发，以低于角色跑速的小步长走正常 `input` 通道；未被确认
  的一步只会重试，不会累积成本地假位置，服务端仍执行限速、防瞬移、边界、碰撞和门口
  距离校验。斑马线、步道、场馆入口及门前接近点直接读取
  `shared/src/cityplan.ts`；室内返回路线读取共享布局的出口。移动建筑或房门时不需要再
  维护散落在三个脚本里的坐标，坐过椅子后先走正常 `stand` 协议再移动；影院最高排
  验收也从共享布局选择最高座位，并走服务端 `sit` 距离校验和客户端高度吸附。
- **NPC 抢占轰趴馆入口提示**：Rin 的巡游线移动到街道南侧，北侧轰趴馆门前恢复净空；
  角色本身的模型、动画和控制逻辑未改动。
- **联机断言依赖固定等待**：媒体旅程不再假设广播会在固定 `1.2s` 内到达所有后台页；
  `set/seek/pause/rate/play/clear` 都等待两端观察到对应权威状态后再采样，避免把云端调度
  延迟误报成同步漂移。
- **街道只改色而未改结构**：一番街改为共享数据驱动的紧凑十字路口，四组斑马线、八段
  短步道、四角贴边楼体和三条场馆路线都由 `shared/src/cityplan.ts` 提供；云端旅程不再
  根据旧直街或 T 字路口猜路径。
- **转角楼尺寸与朝向不一致**：旋转建筑统一通过 `cityBuildingLocalSize()` 把世界碰撞
  尺寸换算成立面宽度/纵深；店面、住宅、前景店招和服务端碰撞共享同一矩形契约。
- **高楼只有体量、缺少近景信息密度**：所有建筑类型都能消费数据化多层招牌；大型屏、
  贴墙横牌与垂直刀牌由共享配置驱动，保持原创文字和图形，不在渲染组件中散落坐标。
- **云端截图异常被静默忽略**：云端旅程必须产出桌面十字路口、轰趴馆、影院最高排和
  移动端十字路口截图；截图失败计入旅程失败，不能再用可选 `catch` 让功能全绿却没有
  视觉证据。
- **西南出生点首屏只见转角楼侧墙**：出生点移到南侧斑马线，中轴镜头直接看向十字路口、
  街旗和两侧高层招牌；这属于共享布局而不是测试专用镜头，真实玩家与云端截图使用同一
  首屏构图。
- **默认镜头俯拍导致高楼被裁掉**：户外空间使用轻微仰视、距离稍远的初始第三人称镜头，
  压低路面占比并让媒体塔、街旗和高位招牌进入首屏；室内仍使用原来的近距离俯视参数。
- **北侧消失点是无信息的灰色楼墙**：道路尽端收紧到可玩边界外 1 米，并改成低位大幅
  原创媒体立面；近处街旗、两侧刀牌和尽端大屏形成前中后三层信息，首屏中央不再残留
  大块空白墙，也不依赖随机楼群碰运气。
- **云端旅程从任意门口误用出生路线**：场馆路线契约保持“出生点到入口”，从轰趴馆返回
  后先反向走同一共享路线回出生点，再前往影院，自动化不再用偶然可走的对角线穿街。
- **街道 HUD 仍像圆角玻璃后台**：户外 HUD 改成独立的非对称海报层；区域、任务、地点、
  路线和详情以斜切、错位印刷、局部网点和撕纸边缘组织，地点按钮继续是可聚焦的 DOM
  元素，室内功能面板不受影响。
- **首轮海报路线图盖住角色和路面**：路线信息缩到右下背景层并降低不透明度；手机端隐藏
  该 SVG 图层与速度纹理，只保留分散地点标签、当前目标和可操作按钮，不退化成卡片列表。
- **媒体塔被街角小店和默认中轴镜头遮挡**：小店并入媒体塔首层，巨幕降到商业街视线，
  出生镜头轻微转向东北；远端尽头改为黄/青次级信息，红色主焦点回到街角地标。
- **首版“动态海报”仍是低多边形街道加大块 HUD**：以云端实拍而不是源代码作为判断依据，
  扩大路口媒体塔和原创巨幕、增加首层连续橱窗与中层窗带、加入只占一份合并网格的远处
  行人层，并把摄影目标抬到招牌高度；导航详情缩到右下，玩家与路面重新成为可见主体。
- **横屏看见巨幕但首层留有干净白墙、竖屏裁掉地标**：媒体塔首层增加数据化商业门楣，
  出生镜头进一步向东北地标偏转，任务纸条收窄；这些是所有真实玩家共用的布局与摄影
  参数，不是云端脚本专用截图角度。

### 当前仍存在的不足与后续要求

以下项目是已知边界，不得在界面、README 或发布说明里包装成已经完成：

1. **飞行棋还不是联机玩法（P1）**：目前只有棋盘、棋子和四个围坐点。后续实现时要在
   `shared` 定义状态/指令、由 `server` 判定掷骰和行棋、按房间广播，并增加断线重连、
   旁观和云端双人流程；禁止把规则塞进 React 组件。
2. **任意网页 iframe 无法保证同步（平台边界）**：跨域页面不暴露可靠媒体控制接口。
   只能明确提示“网页展示不等于同步播放”，共同看片优先使用已适配 Provider。
3. **进度条总时长依赖本地元数据（P1）**：当前位置已经统一权威，但视频总时长仍由
   Provider 本地上报。后续 Provider Adapter 应提供可靠 duration，并处理直播流、
   元数据失败和不同 CDN 返回不一致的情况。
4. **旧空间构建器仍在主布局文件（P2）**：咖啡馆、旧游戏厅、商店和塔楼大堂为历史
   玩法与个人房间链路保留，但没有街道入口。完成存档迁移方案后，应拆到
   `shared/src/layouts/legacy.ts`，主文件只保留一番街、三场馆和房间壳。
5. **美术验收仍需人工看图（P1）**：云端旅程会生成截图并验证坐标/交互，但当前没有
   稳定的像素基准。每次修改街道构图、日照、影院台阶或轰趴馆家具后，应检查 Actions
   上传的桌面与移动端截图，确认不穿模、不浮空、不过曝且主通道清楚。
6. **设备性能预算缺少长期数据（P2）**：密集招牌、轮廓线和 Bloom 已受控，但尚无真实
   低端设备帧时间统计。后续应在云端旅程记录场景 draw call、首屏时间和长帧比例，
   再决定是否增加质量档，而不是散落设备判断。
7. **高层街谷尚无像素回归基准（P1）**：云端已强制保留桌面/移动截图，但目前仍需人工
   核对相机遮挡、招牌裁切和天空占比。积累稳定样本后可增加宽容的视觉差异阈值；后续只
   调整相机与数据化立面参数，不复制场景组件或为单一机型增加分叉布局。

维护规则：优先消除“一个概念多份真值”、散落魔法数字和客户端自判联机结果；公共坐标、
协议、状态公式和玩法规则必须分别归属 `shared`/`server` 的明确模块，表现组件只消费数据。

## 6. 后续增量需求记录格式

后续需求按下面格式追加，保留历史，不覆盖本基线：

```md
### YYYY-MM-DD：需求标题

- 用户目标：
- 影响空间：
- 新增内容：
- 保持不变：
- 联机/服务端规则：
- 美术参考与原创边界：
- 云端验收：
- 资源或依赖：
```

## 7. 新内容接入清单

新增一个场馆或街道内容时，至少检查：

1. 在本文件“增量需求记录”增加需求条目。
2. 在 `shared/src/cityplan.ts` 或 `shared/src/layouts.ts` 增加唯一坐标和交互数据。
3. 在 `client/src/world3d/spaces/registry.tsx` 注册程序化预制件或仓库内资源。
4. 若有多人状态，在 `shared` 协议和 `server` 权威逻辑中实现，客户端只作预测与表现。
5. 更新 `shared/test`、`server/test` 和 `scripts/*.mjs` 的云端验收路径。
6. 只推送到当前账户的 `godluo666/V2`，由 `.github/workflows/cloud-tests.yml` 验证。

## 8. 增量需求记录

### 2026-07-30：紧凑绚烂的一番街与三场馆

- 用户目标：街道不再过大，形成紧凑住宅建筑围合的一条街。
- 影响空间：户外一番街、电影院、团子轰趴馆、电竞观战馆。
- 新增内容：原创漫画都市街头视觉、影院实体台阶、飞行棋桌、社团活动室陈设、
  服务器时钟驱动的统一媒体进度。
- 保持不变：团子角色。
- 联机/服务端规则：房间媒体状态权威同步，修订号防乱序，RTT 中点校时。
- 美术参考与原创边界：参考 P5 涩谷街头的视觉节奏，做得更绚烂；不复制具体资产。
- 云端验收：只在 GitHub Actions 运行类型、单元、服务端、构建和浏览器测试。
- 资源或依赖：全部留在当前仓库。

### 2026-07-31：四角贴边的高层十字街景

- 用户目标：布局结构直接大改为紧凑十字路口；允许高楼大厦，但中央不能空出一大段
  无内容距离，转身即见建筑、店面、行人和招牌。
- 影响空间：户外一番街及三个场馆的门前返回点、出生镜头、NPC 巡游线和云端行走路线。
- 新增内容：四组斑马线、八段短步道、`22–42m` 近景楼体、`28–64m` 远景天际线、
  原创红黑黄街旗、大型媒体牌和垂直刀牌。
- 保持不变：团子角色；仅电影院、团子轰趴馆和电竞观战馆可进入；三个室内的产品基线。
- 联机/服务端规则：不新增客户端自判逻辑；道路、建筑碰撞、入口和自动旅程继续共享
  `cityplan.ts` 的唯一坐标源。
- 美术参考与原创边界：致敬 P5 涩谷街头的高对比、招牌峡谷、高层压景和漫画斜切节奏；
  所有楼名、字体渲染、纹理、街旗和路面图形均为《月汐町》原创程序化内容。
- 云端验收：GitHub Actions 检查边界未扩大、四组斑马线、建筑不压道路、出生点与三条
  门前路线可走，并强制生成桌面十字路口、轰趴馆、影院最高排和移动端截图供人工审视。
- 资源或依赖：未增加外部运行依赖或仓库外资源。

### 2026-07-31：动态都市海报式街景重构

- 用户目标：彻底推翻规则盒子楼和普通后台式 HUD，让可行走街景本身成为高对比、非对称、
  有前中后三层的动态都市海报，并参考补充图片中的密集购物街和路口媒体塔构图。
- 影响空间：户外一番街的建筑渲染、综合色板、出生镜头、跨街旗与 HUD；三座室内场馆
  的玩法和视觉基线不变。
- 新增内容：`mediaTower` 独立建筑类型、椭圆塔身、低位原创媒体巨幕、三道购物街跨街旗、
  深墨高楼色阶，以及使用共享 `VENUES.route` 的海报化地点导航、任务、距离和玩家位置。
- 保持不变：团子角色、移动与门口交互；仅电影院、团子轰趴馆和电竞观战馆可进入；
  影院权威同步、轰趴馆棋类活动和所有室内功能不变。
- 联机/服务端规则：地点点击只标记共享路线，不传送；进入场馆仍必须通过服务器门口距离、
  防瞬移、碰撞和限速检查。
- 美术参考与原创边界：只借鉴高对比、错位印刷、紧凑商店街、夸张媒体塔和快速动势；
  不复制任何角色、图标、字体、地图或纹理，巨幕、招牌、网点和排版均由仓库内代码生成。
- 云端验收：GitHub Actions 运行类型检查、全部单元测试、生产构建和四套 Chromium 旅程；
  桌面及手机截图必须人工检查 HUD 遮挡、目标可读性、媒体塔可见性和触控操作空间。
- 资源或依赖：没有新增运行依赖，所有实现只使用现有 React、Three.js、CSS、SVG 与程序化
  CanvasTexture，并保留 `prefers-reduced-motion` 降级。

### 2026-07-31：以云端实拍为硬标准的街景美术返工

- 用户目标：不能只在需求描述或 HUD 上接近参考；GitHub Actions 产出的实际游戏截图必须
  第一眼呈现高密度商业街、低机位高楼、媒体广告包围和高对比漫画都市节奏。
- 影响空间：一番街媒体塔、电影院外立面、默认第三人称摄影、街道人群和海报导航占屏比例。
- 新增内容：扩大并前置的媒体塔巨幕、首层店铺与中层窗带、十四名低成本远景行人、
  更亮的街道综合色板、右下紧凑路线详情以及横竖屏共同使用的地标取景方向。
- 保持不变：玩家团子模型、动画、移动、三座场馆入口、影院同步、棋类活动和室内摄影参数。
- 联机/服务端规则：行人层仅为静态远景，不产生交互或客户端碰撞真值；场馆坐标、路线、
  入口距离与返回点仍由共享数据和服务端验证。
- 美术参考与原创边界：参考用户图片的密集购物街、路口媒体塔、仰视高楼和广告包围关系；
  所有屏幕图形、文字、人物、楼体和颜色均为原创程序化实现，不复制游戏素材或角色。
- 云端验收：每次构图提交都先通过类型、单元、生产构建和真实多人旅程，再下载桌面和移动
  截图人工复核；测试全绿但实拍仍像空旷低模场景时必须继续修改。
- 资源或依赖：未增加包依赖、CDN、远程图片或仓库外运行资源。

### 2026-08-06：高层转角立面与场馆内部体量升级

- 用户目标：街景继续保持紧凑绚烂的高楼夹缝感；电影院必须像有氛围的巨幕厅，电竞观战馆要
  具备参考图中大型赛事场馆的气势，不再像小型普通房间。
- 影响空间：户外 shopfront/mediaTower/crowd 建模、电影院室内、电竞观战馆室内与联赛主屏。
- 新增内容：转角侧立面窗带、场馆门头灯箱、媒体塔包屏、带发型/手臂/鞋面/手机的远景行人；
  影院 24×10 巨幕舞台框、吸音板、双主过道灯带、顶棚星点；观战馆侧向阶梯看台、中央赛台、
  悬浮信息屏、灯架和紫青粉聚光束，联赛主屏扩大为横跨北墙的 15.6×6.6 屏。
- 保持不变：团子角色、座位和门口交互、影院唯一媒体播放器与房间同步、观战馆 8 个可坐机位、
  现有路线与服务端权威状态；所有新增几何仍为代码内程序化材质。
- 联机/服务端规则：只增加静态表现层和主屏尺寸，不新增客户端权威；座位高度、屏幕 ID、媒体
  策略和交互坐标继续由 shared/layouts.ts 与 registry 共同维护。
- 美术参考与原创边界：抽象借鉴用户提供的赛事场馆灯光、看台、中央舞台层级，以及 P5 涩谷的
  高对比都市节奏；不复制具体队标、角色、字体、地图、图标或图片素材。
- 云端验收：不得在本地运行测试；推送后只使用 GitHub Actions 的类型检查、单元测试、生产构建
  和 Chromium 桌面/移动旅程，并人工查看影院、观战馆和街景截图，重点检查座椅接地、屏幕不穿墙、
  看台不堵交互路线、移动端可达性和不过曝。
- 资源或依赖：仅使用当前仓库已有 React、Three.js、程序化 CanvasTexture 与材质工厂，不新增
  npm 依赖、CDN 或仓库外资源。
