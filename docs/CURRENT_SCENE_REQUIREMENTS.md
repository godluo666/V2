# 《月汐町》当前场景需求基线

> 状态：已确认  
> 基线日期：2026-07-31
> 当前实现分支：`agent/sunny-town`

这份文件记录已经确认的场景方向和不可回退项。后续增加场馆、玩法、家具或活动时，
先在“增量需求记录”追加条目，再修改共享布局、客户端表现、服务端规则和云端验收。

## 1. 不可回退的产品约束

- 保持现有团子角色的模型、配色能力、动画、控制手感和联机表现不变。
- 所有运行依赖、脚本、资源声明和测试依赖只能位于当前仓库内；不依赖全局安装或仓库外文件。
- 不在本地运行测试、构建或浏览器验收。验证统一交给 GitHub Actions 云端工作区。
- 当前首阶段街区只保留三个可进入场馆：
  - 星汐电影院
  - 团子轰趴馆
  - 镜界电竞观战馆
- 旧咖啡馆、便利店、游戏厅、团子塔和车站不出现在当前街道，也不提供入口。

## 2. 一番街户外基线

- 街道参考 P5 涩谷街头的“高对比、斜向构图、密集招牌、漫画都市节奏”，但不复制其
  商标、角色、字体、地图、贴图或具体美术资产；最终视觉必须是《月汐町》的原创表达。
- 画面要绚烂、明媚、有阳光，同时保持角色、入口、路面和交互提示清晰可读。
- 使用 `58m × 46m` 可玩边界；`7.2m` 宽的东西主街和同宽南北短街组成紧凑十字路口。
  路口图形区仅 `10m × 7.2m`，不再用大型十字广场、长支路或空旷车站区域制造距离。
- 四个街角从路缘起即由 `22–42m` 的近景高楼、场馆和住宅立面围合，中间只保留车辆与
  行人通行所需面积；不允许建筑后退形成无内容的大块空地。
- 三个场馆嵌在连续高层立面里，住宅、公寓、阳台、雨棚、海报、电线、自行车和花槽
  共同形成“紧凑的一条街”。
- 路面使用深墨沥青作为构图底色，以红、青、黄的小面积图形、招牌和灯带提亮。
- 入口前、出生点和主步行带保持净空；装饰物靠步道边缘成组布置。
- 户外服务端碰撞、客户端渲染和云端浏览器路径的唯一坐标来源是
  `shared/src/cityplan.ts`，不得另写一套散落坐标。

## 3. 三个场馆基线

### 星汐电影院

- 保留巨幕与 50 个座位，座区采用 5 排、三段分组和宽过道。
- 椅脚必须落在地面或对应排的台阶顶面；台阶高度、椅子原点、坐下吸附点和角色高度
  使用同一份共享布局数据，禁止仅靠视觉偏移修补浮空。
- 视频、YouTube 和控制栏必须显示同一个房间权威播放状态。

### 团子轰趴馆

- 氛围参考温馨社团活动室：奶油墙面、暖木地板、软毯、围坐沙发、茶水零食台、
  阅读角、社团活动板、照片奖杯墙、小舞台和点歌机。
- 保留可玩的服务端权威象棋桌。
- 放置一张有四方座位和棋子的飞行棋桌。首阶段它是可围坐的实体桌游陈设；
  若以后增加在线规则，必须走服务端权威状态，不能伪装成已联机玩法。
- 家具沿墙或成组放置，中部和入口保留通行空间，整体温馨但不拥挤。

### 镜界电竞观战馆

- 保留中央联赛大屏、观战区、机位和清晰主通道。
- 使用蓝青强调光配合暖色前台，避免压抑的纯黑网吧效果。
- 大屏共享播放状态与电影院使用同一套媒体同步基础设施。

## 4. 媒体同步基线

- 参考 VideoTogether 的同步思想：房间状态携带播放时间、暂停状态、播放速度和更新时间；
  客户端通过请求往返时间中点估计服务器时钟偏移。
- 服务端是媒体状态、修订号和房间广播的唯一权威来源。
- 所有玩家的进度条根据“服务器时间 + 权威媒体锚点”计算，不读取各自视频元素的本地
  `currentTime` 作为 UI 真值。
- 客户端忽略旧修订，断线重连和晚加入时使用最新快照恢复。
- 播放中的漂移策略：
  - `≤ 150ms`：保持正常速率；
  - `150–500ms`：支持细粒度速率的平台用约 `±3%` 的短时微调；
  - `> 500ms`：跳转到权威时间；
  - 暂停状态出现超过 `150ms` 的偏差时直接对齐。
- 缓冲期间不反复跳转；恢复可播放后重新对齐。
- 任意跨域网页 iframe 内的视频无法由宿主页可靠控制，不得虚假宣称同步。一起看内容
  应使用官方 YouTube 适配器或直接视频地址；新增提供方时实现统一 Provider Adapter。

## 5. 本轮完成后的维护性审计

### 已在第二轮优化中收口

- **同步策略重复**：直接视频和 YouTube 不再各自维护一套漂移阈值，统一调用
  `client/src/world3d/media/provider.ts` 的纯函数校正计划；新增 Provider 必须复用它。
- **首次进入可能延迟对齐**：播放器首挂载允许立即跳到权威时间，只有后续连续跳转受冷却限制。
- **YouTube 地址形态单一**：统一解析 `watch`、`youtu.be`、`shorts`、`live` 和
  `youtube-nocookie/embed`，解析规则有独立云端单测。
- **场馆入口信息重复**：入口名称、空间键和坐标统一从 `VENUES` 生成，增加场馆时不再
  同时维护第二张映射表。
- **当前城市被旧设施常量污染**：车站和天桥坐标已从 `shared/src/cityplan.ts` 移除；
  尚未删除的旧程序化预制件只保留本地归档尺寸，不再参与共享城市契约。
- **模块初始化隐式修改数组**：远景楼群改为纯函数生成后一次性合并，避免导入模块时
  对导出数组执行 `push`。
- **无入口旧房间的返回坐标失效**：兼容保留的旧房间统一回到一番街安全出生点，
  避免历史会话或内部跳转回到新边界之外。
- **浏览器旅程看见提示却不互动**：门和银幕交互统一走
  `scripts/browser-driver.mjs`；每次移动后必须重新检查提示、按下真实的 `E`，并等待目标
  空间确认，避免最后一次移动后漏检造成十几分钟的级联假失败。
- **云端低帧率让按键行走超时**：云端浏览器不再把 Three.js 渲染帧率当作移动时钟；
  旅程从最新服务端自身快照出发，以低于角色跑速的小步长走正常 `input` 通道；未被确认
  的一步只会重试，不会累积成本地假位置，服务端仍执行限速、防瞬移、边界、碰撞和门口
  距离校验。斑马线、步道、场馆入口及门前接近点直接读取
  `shared/src/cityplan.ts`；室内返回路线读取共享布局的出口。移动建筑或房门时不需要再
  维护散落在三个脚本里的坐标，坐过椅子后先走正常 `stand` 协议再移动；影院最高排
  验收也从共享布局选择最高座位，并走服务端 `sit` 距离校验和客户端高度吸附。
- **NPC 抢占轰趴馆入口提示**：Rin 的巡游线移动到街道南侧，北侧轰趴馆门前恢复净空；
  角色本身的模型、动画和控制逻辑未改动。
- **联机断言依赖固定等待**：媒体旅程不再假设广播会在固定 `1.2s` 内到达所有后台页；
  `set/seek/pause/rate/play/clear` 都等待两端观察到对应权威状态后再采样，避免把云端调度
  延迟误报成同步漂移。
- **街道只改色而未改结构**：一番街改为共享数据驱动的紧凑十字路口，四组斑马线、八段
  短步道、四角贴边楼体和三条场馆路线都由 `shared/src/cityplan.ts` 提供；云端旅程不再
  根据旧直街或 T 字路口猜路径。
- **转角楼尺寸与朝向不一致**：旋转建筑统一通过 `cityBuildingLocalSize()` 把世界碰撞
  尺寸换算成立面宽度/纵深；店面、住宅、前景店招和服务端碰撞共享同一矩形契约。
- **高楼只有体量、缺少近景信息密度**：所有建筑类型都能消费数据化多层招牌；大型屏、
  贴墙横牌与垂直刀牌由共享配置驱动，保持原创文字和图形，不在渲染组件中散落坐标。
- **云端截图异常被静默忽略**：云端旅程必须产出桌面十字路口、轰趴馆、影院最高排和
  移动端十字路口截图；截图失败计入旅程失败，不能再用可选 `catch` 让功能全绿却没有
  视觉证据。
- **西南出生点首屏只见转角楼侧墙**：出生点移到南侧斑马线，中轴镜头直接看向十字路口、
  街旗和两侧高层招牌；这属于共享布局而不是测试专用镜头，真实玩家与云端截图使用同一
  首屏构图。
- **默认镜头俯拍导致高楼被裁掉**：户外空间使用轻微仰视、距离稍远的初始第三人称镜头，
  压低路面占比并让媒体塔、街旗和高位招牌进入首屏；室内仍使用原来的近距离俯视参数。
- **北侧消失点是无信息的灰色楼墙**：道路尽端收紧到可玩边界外 1 米，并改成低位大幅
  原创媒体立面；近处街旗、两侧刀牌和尽端大屏形成前中后三层信息，首屏中央不再残留
  大块空白墙，也不依赖随机楼群碰运气。
- **云端旅程从任意门口误用出生路线**：场馆路线契约保持“出生点到入口”，从轰趴馆返回
  后先反向走同一共享路线回出生点，再前往影院，自动化不再用偶然可走的对角线穿街。
- **街道 HUD 仍像圆角玻璃后台**：户外 HUD 改成独立的非对称海报层；区域、任务、地点、
  路线和详情以斜切、错位印刷、局部网点和撕纸边缘组织，地点按钮继续是可聚焦的 DOM
  元素，室内功能面板不受影响。
- **首轮海报路线图盖住角色和路面**：路线信息缩到右下背景层并降低不透明度；手机端隐藏
  该 SVG 图层与速度纹理，只保留分散地点标签、当前目标和可操作按钮，不退化成卡片列表。
- **媒体塔被街角小店和默认中轴镜头遮挡**：小店并入媒体塔首层，巨幕降到商业街视线，
  出生镜头轻微转向东北；远端尽头改为黄/青次级信息，红色主焦点回到街角地标。
- **首版“动态海报”仍是低多边形街道加大块 HUD**：以云端实拍而不是源代码作为判断依据，
  扩大路口媒体塔和原创巨幕、增加首层连续橱窗与中层窗带、加入只占一份合并网格的远处
  行人层，并把摄影目标抬到招牌高度；导航详情缩到右下，玩家与路面重新成为可见主体。
- **横屏看见巨幕但首层留有干净白墙、竖屏裁掉地标**：媒体塔首层增加数据化商业门楣，
  出生镜头进一步向东北地标偏转，任务纸条收窄；这些是所有真实玩家共用的布局与摄影
  参数，不是云端脚本专用截图角度。

### 当前仍存在的不足与后续要求

以下项目是已知边界，不得在界面、README 或发布说明里包装成已经完成：

1. **飞行棋还不是联机玩法（P1）**：目前只有棋盘、棋子和四个围坐点。后续实现时要在
   `shared` 定义状态/指令、由 `server` 判定掷骰和行棋、按房间广播，并增加断线重连、
   旁观和云端双人流程；禁止把规则塞进 React 组件。
2. **任意网页 iframe 无法保证同步（平台边界）**：跨域页面不暴露可靠媒体控制接口。
   只能明确提示“网页展示不等于同步播放”，共同看片优先使用已适配 Provider。
3. **进度条总时长依赖本地元数据（P1）**：当前位置已经统一权威，但视频总时长仍由
   Provider 本地上报。后续 Provider Adapter 应提供可靠 duration，并处理直播流、
   元数据失败和不同 CDN 返回不一致的情况。
4. **旧空间构建器仍在主布局文件（P2）**：咖啡馆、旧游戏厅、商店和塔楼大堂为历史
   玩法与个人房间链路保留，但没有街道入口。完成存档迁移方案后，应拆到
   `shared/src/layouts/legacy.ts`，主文件只保留一番街、三场馆和房间壳。
5. **美术验收仍需人工看图（P1）**：云端旅程会生成截图并验证坐标/交互，但当前没有
   稳定的像素基准。每次修改街道构图、日照、影院台阶或轰趴馆家具后，应检查 Actions
   上传的桌面与移动端截图，确认不穿模、不浮空、不过曝且主通道清楚。
6. **设备性能预算缺少长期数据（P2）**：密集招牌、轮廓线和 Bloom 已受控，但尚无真实
   低端设备帧时间统计。后续应在云端旅程记录场景 draw call、首屏时间和长帧比例，
   再决定是否增加质量档，而不是散落设备判断。
7. **高层街谷尚无像素回归基准（P1）**：云端已强制保留桌面/移动截图，但目前仍需人工
   核对相机遮挡、招牌裁切和天空占比。积累稳定样本后可增加宽容的视觉差异阈值；后续只
   调整相机与数据化立面参数，不复制场景组件或为单一机型增加分叉布局。

维护规则：优先消除“一个概念多份真值”、散落魔法数字和客户端自判联机结果；公共坐标、
协议、状态公式和玩法规则必须分别归属 `shared`/`server` 的明确模块，表现组件只消费数据。

## 6. 后续增量需求记录格式

后续需求按下面格式追加，保留历史，不覆盖本基线：

```md
### YYYY-MM-DD：需求标题

- 用户目标：
- 影响空间：
- 新增内容：
- 保持不变：
- 联机/服务端规则：
- 美术参考与原创边界：
- 云端验收：
- 资源或依赖：
```

## 7. 新内容接入清单

新增一个场馆或街道内容时，至少检查：

1. 在本文件“增量需求记录”增加需求条目。
2. 在 `shared/src/cityplan.ts` 或 `shared/src/layouts.ts` 增加唯一坐标和交互数据。
3. 在 `client/src/world3d/spaces/registry.tsx` 注册程序化预制件或仓库内资源。
4. 若有多人状态，在 `shared` 协议和 `server` 权威逻辑中实现，客户端只作预测与表现。
5. 更新 `shared/test`、`server/test` 和 `scripts/*.mjs` 的云端验收路径。
6. 只推送到当前账户的 `godluo666/V2`，由 `.github/workflows/cloud-tests.yml` 验证。

## 8. 增量需求记录

### 2026-07-30：紧凑绚烂的一番街与三场馆

- 用户目标：街道不再过大，形成紧凑住宅建筑围合的一条街。
- 影响空间：户外一番街、电影院、团子轰趴馆、电竞观战馆。
- 新增内容：原创漫画都市街头视觉、影院实体台阶、飞行棋桌、社团活动室陈设、
  服务器时钟驱动的统一媒体进度。
- 保持不变：团子角色。
- 联机/服务端规则：房间媒体状态权威同步，修订号防乱序，RTT 中点校时。
- 美术参考与原创边界：参考 P5 涩谷街头的视觉节奏，做得更绚烂；不复制具体资产。
- 云端验收：只在 GitHub Actions 运行类型、单元、服务端、构建和浏览器测试。
- 资源或依赖：全部留在当前仓库。

### 2026-07-31：四角贴边的高层十字街景

- 用户目标：布局结构直接大改为紧凑十字路口；允许高楼大厦，但中央不能空出一大段
  无内容距离，转身即见建筑、店面、行人和招牌。
- 影响空间：户外一番街及三个场馆的门前返回点、出生镜头、NPC 巡游线和云端行走路线。
- 新增内容：四组斑马线、八段短步道、`22–42m` 近景楼体、`28–64m` 远景天际线、
  原创红黑黄街旗、大型媒体牌和垂直刀牌。
- 保持不变：团子角色；仅电影院、团子轰趴馆和电竞观战馆可进入；三个室内的产品基线。
- 联机/服务端规则：不新增客户端自判逻辑；道路、建筑碰撞、入口和自动旅程继续共享
  `cityplan.ts` 的唯一坐标源。
- 美术参考与原创边界：致敬 P5 涩谷街头的高对比、招牌峡谷、高层压景和漫画斜切节奏；
  所有楼名、字体渲染、纹理、街旗和路面图形均为《月汐町》原创程序化内容。
- 云端验收：GitHub Actions 检查边界未扩大、四组斑马线、建筑不压道路、出生点与三条
  门前路线可走，并强制生成桌面十字路口、轰趴馆、影院最高排和移动端截图供人工审视。
- 资源或依赖：未增加外部运行依赖或仓库外资源。

### 2026-07-31：动态都市海报式街景重构

- 用户目标：彻底推翻规则盒子楼和普通后台式 HUD，让可行走街景本身成为高对比、非对称、
  有前中后三层的动态都市海报，并参考补充图片中的密集购物街和路口媒体塔构图。
- 影响空间：户外一番街的建筑渲染、综合色板、出生镜头、跨街旗与 HUD；三座室内场馆
  的玩法和视觉基线不变。
- 新增内容：`mediaTower` 独立建筑类型、椭圆塔身、低位原创媒体巨幕、三道购物街跨街旗、
  深墨高楼色阶，以及使用共享 `VENUES.route` 的海报化地点导航、任务、距离和玩家位置。
- 保持不变：团子角色、移动与门口交互；仅电影院、团子轰趴馆和电竞观战馆可进入；
  影院权威同步、轰趴馆棋类活动和所有室内功能不变。
- 联机/服务端规则：地点点击只标记共享路线，不传送；进入场馆仍必须通过服务器门口距离、
  防瞬移、碰撞和限速检查。
- 美术参考与原创边界：只借鉴高对比、错位印刷、紧凑商店街、夸张媒体塔和快速动势；
  不复制任何角色、图标、字体、地图或纹理，巨幕、招牌、网点和排版均由仓库内代码生成。
- 云端验收：GitHub Actions 运行类型检查、全部单元测试、生产构建和四套 Chromium 旅程；
  桌面及手机截图必须人工检查 HUD 遮挡、目标可读性、媒体塔可见性和触控操作空间。
- 资源或依赖：没有新增运行依赖，所有实现只使用现有 React、Three.js、CSS、SVG 与程序化
  CanvasTexture，并保留 `prefers-reduced-motion` 降级。

### 2026-07-31：以云端实拍为硬标准的街景美术返工

- 用户目标：不能只在需求描述或 HUD 上接近参考；GitHub Actions 产出的实际游戏截图必须
  第一眼呈现高密度商业街、低机位高楼、媒体广告包围和高对比漫画都市节奏。
- 影响空间：一番街媒体塔、电影院外立面、默认第三人称摄影、街道人群和海报导航占屏比例。
- 新增内容：扩大并前置的媒体塔巨幕、首层店铺与中层窗带、十四名低成本远景行人、
  更亮的街道综合色板、右下紧凑路线详情以及横竖屏共同使用的地标取景方向。
- 保持不变：玩家团子模型、动画、移动、三座场馆入口、影院同步、棋类活动和室内摄影参数。
- 联机/服务端规则：行人层仅为静态远景，不产生交互或客户端碰撞真值；场馆坐标、路线、
  入口距离与返回点仍由共享数据和服务端验证。
- 美术参考与原创边界：参考用户图片的密集购物街、路口媒体塔、仰视高楼和广告包围关系；
  所有屏幕图形、文字、人物、楼体和颜色均为原创程序化实现，不复制游戏素材或角色。
- 云端验收：每次构图提交都先通过类型、单元、生产构建和真实多人旅程，再下载桌面和移动
  截图人工复核；测试全绿但实拍仍像空旷低模场景时必须继续修改。
- 资源或依赖：未增加包依赖、CDN、远程图片或仓库外运行资源。

### 2026-08-06：高层转角立面与场馆内部体量升级

- 用户目标：街景继续保持紧凑绚烂的高楼夹缝感；电影院必须像有氛围的巨幕厅，电竞观战馆要
  具备参考图中大型赛事场馆的气势，不再像小型普通房间。
- 影响空间：户外 shopfront/mediaTower/crowd 建模、电影院室内、电竞观战馆室内与联赛主屏。
- 新增内容：转角侧立面窗带、场馆门头灯箱、媒体塔包屏、带发型/手臂/鞋面/手机的远景行人；
  影院 24×10 巨幕舞台框、吸音板、双主过道灯带、顶棚星点；观战馆侧向阶梯看台、中央赛台、
  悬浮信息屏、灯架和紫青粉聚光束，联赛主屏扩大为横跨北墙的 15.6×6.6 屏。
- 保持不变：团子角色、座位和门口交互、影院唯一媒体播放器与房间同步、观战馆 8 个可坐机位、
  现有路线与服务端权威状态；所有新增几何仍为代码内程序化材质。
- 联机/服务端规则：只增加静态表现层和主屏尺寸，不新增客户端权威；座位高度、屏幕 ID、媒体
  策略和交互坐标继续由 shared/layouts.ts 与 registry 共同维护。
- 美术参考与原创边界：抽象借鉴用户提供的赛事场馆灯光、看台、中央舞台层级，以及 P5 涩谷的
  高对比都市节奏；不复制具体队标、角色、字体、地图、图标或图片素材。
- 云端验收：不得在本地运行测试；推送后只使用 GitHub Actions 的类型检查、单元测试、生产构建
  和 Chromium 桌面/移动旅程，并人工查看影院、观战馆和街景截图，重点检查座椅接地、屏幕不穿墙、
  看台不堵交互路线、移动端可达性和不过曝。
- 资源或依赖：仅使用当前仓库已有 React、Three.js、程序化 CanvasTexture 与材质工厂，不新增
  npm 依赖、CDN 或仓库外资源。

### 2026-08-06：赛博朋克街谷实体建模与写实复核

- 用户目标：街景不能只靠平面颜色和 HUD 像赛博朋克；需要真正的都市结构、材质层次和可被相机
  看到的实体建模，墙体不能大面积光滑空白。
- 影响空间：户外一番街十字路口、建筑转角、街道上空和路面；不改变三场馆入口与可玩边界。
- 新增内容：跨街高架连廊、玻璃栏板、消防梯与冷凝机组、电缆 TubeGeometry、实体霓虹广告箱、
  全息信号柱、湿路面导光槽和低矮反射色块；与楼层压条、窗框、侧墙分缝和媒体塔石材板缝一起
  形成可维护的赛博都市构件层。
- 保持不变：团子角色、街道路线、碰撞、入口和既有同步逻辑；赛博层只负责表现，不创建第二份
  道路或服务端状态真值。
- 美术参考与原创边界：参考赛博朋克都市的霓虹、机电暴露、广告密度、立体交通与湿地面反射，
  不复制任何具体游戏的建筑、字体、角色或图标；所有文字和贴图由仓库内 CanvasTexture 生成。
- 云端验收：仅在 GitHub Actions 进行类型、单元、构建和 Chromium 桌面/移动截图；人工检查高架
  不堵路、霓虹不过曝、电缆不穿镜头主体、墙体有结构阴影、手机端仍能识别三个入口。
- 资源或依赖：不增加依赖，继续使用现有 Three.js 几何、材质工厂和程序化画布。

### 2026-08-06：云端截图诊断——从霓虹原型转向实体建筑与材质

以下诊断来自 GitHub Actions 运行 `31113427346` 产出的桌面街景/影院截图和移动端街景截图；
该运行的类型、单元、构建、影院同步、座位接地和移动端旅程通过，watch-test 的全屏第 2 轮是
既有偶发检查，不影响本次视觉诊断。

1. **大平面/单一长方体**：媒体塔首层台座、南侧公寓和影院相邻店面仍以完整盒体为主，
   远景剪影也能读成规则长方体；原有压条虽然存在，但没有改变主体轮廓。
2. **贴墙薄片**：高层点阵窗、暖窗、部分店铺招牌与跨街装饰主要是 PlaneGeometry 或贴图，
   在云端镜头里没有窗洞、窗台和门框的侧影；已有 sign mesh 有厚度但数量多、构造关系弱。
3. **纯色/自发光材质**：合并建筑、路缘和人行道大量使用顶点色 MeshToonMaterial，缺少
   roughness/metalness/bump；影院座椅、媒体塔 Podium 与金属设备的光照响应几乎一致。
4. **阴影/AO 不足**：主光阴影已打开，但环境光和纯色材质冲淡了建筑底部、招牌、阳台与路缘
   的接触阴影；默认 High 档未启用 SSAO，局部彩色灯没有稳定地形成墙面投影。
5. **轮廓与设备**：消防梯、空调、排水管和电缆多在近景缺席；移动端主要看到平墙和招牌，
   赛博层的文字/霓虹即使存在，也不能替代真实的结构投影。
6. **人体尺度**：人群能提供比例，但影院椅、看台和街边设备仍像缩放后的占位盒，缺少腿部、
   扶手、平台厚度和可见的支撑连接。
7. **代码存在但截图不可读**：已有装饰灯、墙面窗带和部分 HUD 纹理在自由相机或远景中被
   黑色屏幕/高亮遮住；因此本轮验收必须以正常玩家镜头的近景截图为准，而不是只看源码。

本轮实际修正：建立 `city/materials.ts` 程序化微表面材质系统（混凝土、砖、金属、玻璃、
湿沥青、人行道、木材、吸音布、影院地毯和座椅织物），合并城市网格改用带 roughnessMap、
bumpMap、metalness 和 flatShading 的 MeshStandardMaterial；shopfront/apartment 改为
错层主体、实体窗框/侧窗、楼层压条、基础台座和外墙空调机组；媒体塔使用混凝土/金属/玻璃
分类材质；赛博层删去新增文字广告平面和湿地面色块，只保留有支撑的连廊、消防梯、设备平台、
电缆和受控局部灯光。后续截图仍需重点检查这些结构是否在手机镜头中可见。
### 2026-08-06：场馆实体内饰与材质系统复核

- 电影院与电竞观战馆的关键材质已切换到统一的程序化标准材质：金属、拉丝金属、深色玻璃、吸音织物、影院地毯和座椅织物均携带 roughness、metalness、微表面噪声与 bump 响应；霓虹只保留为局部导视和受控光源。
- 电竞观战馆看台不再只有整块台阶：每级增加前沿实体、座椅坐面、靠背、扶手和支撑；中心赛台改为多层厚平台并增加设备箱，主屏增加背壳、侧框、顶部桁架和吊装连接，外围补齐护栏立柱与扶手。
- 影院座椅继续保持座位业务坐标不变，结构包含坐垫、靠背、扶手、杯架、底座和接地关系；巨幕边框、舞台与吸音板仍由实体几何表达。
- 本轮没有新增外部依赖、模型或 Persona 5 素材；仅使用仓库内 Three.js 几何、CanvasTexture 和材质工厂。完成后必须用 GitHub Actions 云端截图复核，不能以本地运行代替。

### 2026-08-07：云端复核后的第二轮结构修正（未宣称最终通过）

- 最新街景截图确认：主要楼体已经恢复为可见的错层/挤出立面，但电影院一侧仍有一块过大的
  单调墙面，说明仅有结构几何还不够，玻璃、金属和混凝土必须在最终合并网格中保持独立材质。
- 本轮代码修正：建筑批处理新增独立 `glass` 与 `metal` 合并层，店铺窗、侧窗、空调机、栏杆
  和设备不再落入混凝土顶点色材质；程序化材质将微表面纹理同时用于 albedo、roughness 和 bump，
  并关闭统一 flatShading，让实体几何在方向光下产生可读的明暗变化。
- 入口稳定性修正：电影院与团子馆的门锚点移到实体立面外侧的人行道边；共享布局的碰撞尺寸显式
  根据四分之一转角换算，避免渲染立面和服务端 AABB 的入口位置不一致。
- 未完成项：必须继续通过云端桌面近景、手机街景、影院座椅、电竞看台和团子馆桌游截图检查；
  若仍看到大面积平墙或材质混用，继续拆分 facade 模块，而不是增加霓虹贴片。

### 2026-08-07：云端浏览器回归复核

- 最新云端截图已能稳定产出街景、影院、电竞馆和团子馆证据；此前的 30 秒室内截图超时来自大面积室内墙体同时启用微表面贴图，已改为保留物理 roughness/metalness、仅对大墙关闭 bump/roughness 采样，立面和道具仍保留微表面。
- 影院入口曾与街边长椅争夺交互焦点，且门点落在建筑碰撞体内；入口已移到建筑外侧并避开长椅，团子馆路线也改为在旋转立面外留出安全距离。
- 手机摇杆在 SwiftShader 低帧下只采到 0.35m，云端证据等待窗口已延长后通过 1m 位移验收；这不是角色模型或移动协议改动。
- 当前仍未宣称最终美术通过：街景截图右侧仍有一块需要继续拆分的连续墙面，影院/电竞馆的截图虽可见实体结构，但暗部和舞台层次仍需人工近景复核；下一轮优先处理这些可见不足。

## 2026-08-09：全场景重新建模美术方向与强制重构范围

本轮不是继续给旧模型增加招牌、灯带、文字或 Bloom，而是按“空间构图 → 主体体块 → 实体构造
→ 材质响应 → 光影 → 镜头 → 图形信息”的顺序重新制作。目标是达到高完成度风格化都市游戏的
结构密度、戏剧构图和材质可信度；抽象借鉴 P5R 的高对比、非对称、都市动势和漫画节奏，禁止
复制其角色、字体、图标、地图、建筑、贴图或 UI。关闭霓虹、文字和后期后，模型本身仍必须成立。

### 统一视觉语言

- 风格采用“风格化写实都市 + 克制漫画图形”：建筑构造和人体尺度可信，轮廓允许适度夸张；
  黑灰与建筑本色承担主体，米白负责信息底，红色只用于目标/入口/路线，青黄紫只做局部城市灯光。
- 漫画拼贴、斜切、速度线、撕纸边和印刷错位主要属于 HUD、入口导视和局部构图，不允许覆盖
  所有墙面来掩盖低模。任何截图在关闭这些图形层后仍要有明确前、中、后景和主次焦点。
- 统一程序化材质必须保持真实比例的粗糙度、金属度、法线/微表面、接缝和轻微色差；混凝土、
  砖、玻璃、拉丝金属、湿沥青、石材、木材、吸音布、地毯和座椅织物必须一眼可区分。
- 户外以阳光主光、天空填充、建筑投影和接触阴影塑造体积；室内建立主亮区、辅助亮区和可读暗部。
  Bloom 只允许作用于少量灯芯，不能替代几何、材质、AO 或投影。

### 街景强制重构

- 保持紧凑十字路口和短街谷，但重新组织四角体量：出生后立即被高楼、首层店面、雨棚、近景设施
  与远景媒体塔围合；中央只保留通行车道，禁止再次形成大广场或长距离无内容空地。
- 建立四个不同剪影的英雄级地标：弧形/折面媒体塔、宏大影院门厅、赛事中心式电竞馆外壳、温暖
  小巷式团子馆。禁止用同一楼体换色换招牌冒充不同建筑。
- 每座主楼必须具有多层错台、首层内凹、实体窗洞和窗框、门廊、梁柱、檐口、楼层分隔、墙板拼缝、
  侧立面、阳台/消防梯、空调与管线平台、屋顶机房/天线/广告支架，以及与人行道连接的台阶和门槛。
- 招牌、巨屏、路牌、雨棚、栏杆、电箱、自动售货机、电缆桥架全部必须有厚度、侧面、底座或支撑；
  PlaneGeometry 仅用于屏幕内容、玻璃反射层和少量海报。
- 街道重做路缘石、排水沟、井盖、局部积水、磨损斑马线、道路补丁、不同人行道铺装、入口台阶、
  自行车区、垃圾/设备区、护栏和路桩。电线、高架与灯架必须通过杆件、吊点或墙体连接，禁止悬空。

### 电影院：布局、大小与模型全部重做

- 旧影院平面和尺寸不作为约束。重建为约 34×30m 的高规格巨幕厅，24m 级银幕嵌入完整前墙，
  同时包含厚幕框、舞台、侧幕、扬声器墙、低音单元、维护结构和投影窗口。
- 观众席改为明显坡度/多级阶梯关系，规划中央与双侧过道、后部集散区和安全出口；入口镜头位于厅后
  偏侧，第一眼同时看到巨幕、阶梯观众席和纵深，而不是只看到椅背或黑墙。
- 单座模型必须包含坐垫、靠背、扶手、杯架、转轴、底座和落地支撑；行列可有克制变化，禁止漂浮、
  穿模或完全机械复制。台阶、过道、地毯和嵌入式过道灯必须共享准确标高。
- 墙面使用有厚度的分块吸音板、木质扩散构件、壁灯和出口门；顶棚加入结构层、灯槽、桁架、音响和
  有限星空灯。保留现有视频同步、暂停、倍速、晚加入恢复、单播放器和座位业务 ID。

### 电竞观战馆：布局、大小与模型全部重做

- 旧网吧式平面和尺寸不作为约束。重建为约 42×34m 的大型赛事观战空间，中央赛台、多层观众看台、
  进出通道、控制区、解说席和后台形成明确层级，正常镜头能同时读出场馆宽度、高度和观众包围感。
- 中央赛台由多层厚平台、台阶、设备舱和线缆入口组成；八个业务机位各自具有实体桌、显示器外壳、
  主机、座椅、隔音板、设备架和线缆，不改变原有机位 ID、座位交互或共享媒体逻辑。
- 主屏、侧屏和比分屏必须有厚外壳、背架、检修结构和支撑；顶部使用完整桁架、吊点、灯具、扬声器
  与电缆桥架，禁止悬空发光条。看台必须具有台阶、座椅、扶手、栏杆、踢脚板和安全通道。
- 紫青为主、少量粉色强调；控制局部光源数量，保留材质可读暗部，不允许用过曝 Bloom 制造气势。

### 团子轰趴馆：温暖精细的社团活动室

- 保持紧凑、温暖、不拥挤，重新组织为围坐客厅、象棋角、飞行棋角、阅读/展示墙和茶水区；家具
  之间保留真实通道与座位关系，不随机堆放重复道具。
- 墙面加入实体书架、储物柜、软木板、照片墙、奖杯架、社团作品和护墙板；沙发包含独立坐垫、靠枕、
  缝线、扶手和底座；棋桌包含棋盘、棋子、抽屉/收纳和围坐关系。
- 木材、墙漆、布艺、金属与塑料保持不同材质响应，使用暖色吊灯、桌灯、窗边自然光和局部阴影制造
  生活感。保留象棋、飞行棋、座位和房间业务逻辑。

### 可维护性、性能与验收

- 将建筑/室内拆为可复用的 `FacadeModule`、`WindowBay`、`EntrancePortal`、`UtilityUnit`、
  `RoofEquipment`、`CinemaSeatRow`、`ArenaStand`、`ClubFurnitureSet` 和 `InteriorLight` 等模块；
  禁止把数百坐标塞回一个超大 TSX。主地标使用独立几何，重复窗、栏杆、灯具和座椅使用实例或复用。
- 不新增仓库外运行依赖、CDN 或来源不明模型/贴图；所有生成纹理、模型与资源必须保存在当前项目内，
  角色模型保持不变。视觉改动不得破坏路由、多人同步、场馆进入、媒体、棋类和座位功能。
- 只在 GitHub Actions 云端执行类型、单元、生产构建和 Chromium 旅程；每轮下载并人工检查桌面街景广角、
  建筑近景、手机街景、影院全景/座椅近景、电竞馆全景/看台机位近景、团子馆全景/桌游家具近景。
- 完成判定：关闭霓虹与后处理仍有完整空间结构；主建筑不再是简单盒子；正侧立面都有实体细节；材质
  明显可分；投影能表达体积；三室内近景可读；无漂浮/穿模；桌面与手机人工截图通过且云端检查全绿。

### 2026-08-09：首轮重建后云端截图审视（未通过）

依据 GitHub Actions 运行 `31304892280` 的实际游戏截图与旅程日志，首轮虽然已通过类型检查、全部单元测试和生产构建，但不得宣称美术验收完成：

1. **街景中心仍有巨大空白平墙**：桌面十字路口截图的中景被一整块灰色墙面占据，缺少楼层分隔、橱窗进退、窗洞和侧立面投影；新建英雄结构在正常出生镜头里没有取代这块旧立面。
2. **电影院门厅近景取景失败**：玩家走到入口后，画面主要是黑墙、天空、长椅与 NPC，实体门廊、冠部、雨棚和建筑侧面不可读。必须同时修正立面前后关系与入口镜头，不得只换截图点。
3. **巨幕厅暗部死黑**：入场宽景几乎只能看到顶部座椅剪影，最高排近景虽确认座椅接地，但幕框、舞台、墙面吸音板、织物座椅和投影室仍难以辨认。需重做可读暗部、墙洗光、台阶光和出生构图，Bloom 不能代替补光。
4. **团子馆仍有“占位盒”感**：功能分区与温暖色温已成立，象棋/飞行棋/沙发/储物墙可见，但椅子、沙发、桌面和柜体的边缘太直，近景仍像基础长方体；需增加倒角软包、层板厚度、桌腿/抽屉/把手和更明确的材质差。
5. **电竞馆两张证据未产出**：影院最高排落座后未先站起，导致后续进入电竞馆的截图分支被跳过。云端旅程必须显式验证站立，并把电竞宽景与赛台近景缺失计为失败，不能再静默跳过。
6. **团子馆返回街道路线在云端低帧下中断**：玩家确实已回到街道空间，但反向共享路线有一段在时限内没有到达。修正必须继续使用真实输入、服务器快照和共享路线，只允许在重新获取输入焦点后重试受渲染卡顿中断的路段；禁止传送或跳过最终距离断言。

第二轮只有在上述问题实际修正、九张证据全部产出、云端旅程全绿并完成人工看图后，才能更新为通过。

### 2026-08-09：第二轮云端截图审视（仍未通过）

依据提交 `de7f148`、GitHub Actions 运行 `31306683577` 的实际截图和日志，本轮类型检查、单元测试与生产构建全部通过；世界、三个场馆、多人媒体和移动端均能启动，但视觉与浏览器验收仍不合格：

1. **街景密度提高但主体仍被黑色大体块截断**：媒体塔首层已经从整块灰墙拆成柱网和橱窗，但跨街连廊与媒体塔屏壳实体相交，桌面首屏仍形成贯穿中心的深色横块；它必须拆分、移位并由真实支撑连接，不能遮住英雄地标。
2. **手机首屏没有建立地标构图**：`390×844` 竖屏下媒体塔贴近右侧边缘且冠部被裁，HUD、线缆和深色前景进一步挤压主体。真实玩家相机必须根据纵横比调整朝向、目标高度与距离，移动端不能只把桌面镜头窄裁。
3. **影院外立面仍不够宏大**：入口已经可识别，门框、雨棚和玻璃也能看到，但右侧交通核仍像高而平的灰色盒子，旧首层墙与新门厅存在叠压风险。下一轮必须继续拆出进退、窗洞、服务槽、侧立面和投影，而不是增加招牌。
4. **影院渲染负担超出云端能力**：宽景截图等待 `90s` 仍超时；静态审计确认影院峰值约有 32 个局部真实灯，并存在一盏六面点光阴影。三馆必须真正消费质量档灯光预算，High 档局部灯上限约 8；重复壁灯/桁架保留实体与 emissive，但不能都成为真实灯，影院不得使用六面点光阴影掩盖材质。
5. **影院暗部仍吞掉座椅材质**：最高排截图能读出幕框、线阵音箱、墙面吸音/扩散构件和座椅落地关系，但织物、背壳、扶手与杯架仍大面积合并成黑色剪影，银幕待机画面几乎全黑。应通过合理暗部反照率、少量有方向主光和原创低亮度待机内容提高可读性，禁止恢复灯海或 Bloom 糊光。
6. **电竞馆广角取景错误**：近景已经能看见巨幕、中央赛台、八机位和桁架，但标记为广角的截图只拍到入口门和看台背面；真实入场出生位置、朝向、FOV 与目标点必须同时展示 42×34m 场馆规模、主屏、赛台和两侧看台。当前暗部也仍吞掉设备壳、栏杆和台阶边缘。
7. **团子馆家具进步但空间仍显空**：沙发软包、椅子支撑、储物柜和桌游关系比首轮清晰，然而宽景中央空地过大、桌游区被裁在边缘，近景只覆盖一小组座椅。应重组围坐客厅、象棋、飞行棋、阅读和茶水区的实际布局与出生构图，保持主通道可走但不留大型无内容地面。
8. **云端证据存在级联误判**：旧脚本把电竞馆流程错误依赖于影院成功状态，且截图前只检查文件非空。后续必须逐场馆核对当前空间、真实寻路结果、WebGL 上下文和渲染首帧；影院失败不得跳过电竞馆，错误空间不得生成带错误标签的截图，九张证据仍全部作为硬门槛。

本轮已确认功能主链没有因重建而丢失：团子馆双人进入/象棋同步/返回街道、影院双人进入与最高排落地、电竞馆进入、共享媒体 seek/暂停/倍速/晚加入恢复、单播放器和移动端移动均有云端日志证据。下一轮必须在保留这些功能的前提下修正上述构图、灯光预算与证据可靠性，并重新完成人工九图审视；在此之前不得宣称达到最终美术水平。

### 2026-08-09：第三轮结构与材质修正（等待云端截图验收）

- **街景不是再加装饰**：删除与媒体塔实体相交的 31m 跨街盒体连廊；媒体塔首层改为内缩核心筒、转换梁、角柱和三面实体橱窗。电影院旧通长首层构件后退，右侧大平墙拆成三层梁柱框、后退玻璃、服务百叶和维护入口。手机出生镜头按纵横比调整目标高度、距离和地标偏置，桌面自由镜头逻辑保持不变。
- **影院材质与光影重排**：保留约 34×30m、24m 巨幕、六级观众席和单播放器契约；座椅增加后背织物嵌件并降低无环境贴图金属面的金属度，幕框、吸音布、地毯与涂装钢分别校准。旧灯海、面积光和六面点阴影已移除，真实局部灯严格服从质量档预算，并用一盏受控方向阴影建立接触关系；待机幕面使用仓库内程序生成的原创低亮度画面。
- **电竞馆布局与落地关系重做**：保持约 42×34m 主赛场和 8 个原业务机位；入场出生点、FOV 与目标点改为同时框入主屏、赛台和两侧看台。主舞台按真实几何拆为 `.60/.55/.40m` 主体与 `.51/.36/.24m` 三级踏步，补齐主屏背壳、落地边柱、侧屏柱和设备柜碰撞；主持台后仍保留可绕行至主屏互动范围的真实通道。
- **团子馆从大空地改为活动室围合**：沙发、双地毯、象棋和飞行棋整体向中轴收拢，新增有倒角桌体、木作支撑、收纳层、活动文件和文具的社团筹备桌及两张业务座位；棋桌、座位 ID、碰撞和云端路线统一读取共享布局，不靠脚本硬编码旧坐标。
- **云端证据不得假通过**：截图前必须同时满足目标空间命名根已提交、WebGL 未丢失、R3F 连续渲染且不存在全屏加载/遮罩；九张图片必须具有完整 PNG chunk、非空 IDAT、最终 IEND 和规定分辨率。浏览器 `console.error`、`pageerror`、真实寻路失败或任一缺图都会使任务失败，同时仍上传已有证据供人工审视。
- **当前仍未完成最终验收**：上述改动尚未经过新一轮 GitHub Actions 类型检查、单元测试、生产构建和九图人工看图，不能提前标记为通过。另有维护性债务：`heroStructures.tsx`、`cinemaHall.tsx`、`arenaHall.tsx` 虽已按职责拆成多个函数并复用批处理/共享几何，但单文件仍偏大；视觉坐标稳定后应继续拆为入口、立面、看台、巨幕、座椅等语义模块，避免后续扩展再次集中堆积。

### 2026-08-09：第三轮云端实拍审视（仍未通过）

依据提交 `cbfc054`、GitHub Actions 运行 `31310508537` 的生产构建、浏览器日志与已上传实拍，本轮类型检查、单元测试和生产构建全部通过；团子馆双人桌游、影院最高排落地、电竞赛台近景及共享媒体单实例均实际运行，但九图与美术验收仍未通过：

1. **街道路口结构成立但前景过密**：移除跨街巨盒后，十字路口、媒体塔骑楼、斑马线和紧凑街墙已经可读；然而低位悬线、中央信号柱和多个 HUD 同时切割主地标，黑色金属在 SwiftShader 下仍吞掉柱网层次。后续应把服务线束抬到店招以上，并提高结构材料的非金属漫反射，而不是增加更多霓虹牌。
2. **影院外立面近景仍有平整灰色门墩**：实体雨棚、内凹门厅、门框和入口台阶已经出现，但近镜左侧门廊柱仍占据大面积平滑浅灰表面。门墩需要增加真实基座、检修嵌板、施工缝、边肋和可投影的小型壁灯外壳，保持入口可用净空不变。
3. **影院内部几何明显进步但银幕仍黑**：最高排截图已经能辨认六级座席、独立背壳/软包、扶手、幕框、帷幕、线阵音箱、声学墙和顶棚桁架，说明重建不是表面贴片；但唯一 DOM 播放层尚未显帧时，24m 银幕仍退化为纯黑。3D backing 必须始终提供低亮度原创待机内容，同时继续保持单播放器。
4. **电竞馆近景过暗且主屏像黑平面**：八机位、桌椅、三级赛台和屏幕外壳能看见，但主屏/侧屏待机面没有视觉内容，深色设备、看台与墙体合并。应提高涂装钢、吸音布和混凝土的中间调，补充实体屏壳分段、背撑和赛台机械层级；真实灯仍受质量档预算约束，禁止恢复灯海或 Bloom 糊光。
5. **团子馆温馨与紧凑已成立，但墙饰朝向错误**：围坐沙发、筹备桌、象棋、飞行棋、木地板和双地毯已形成社团活动室关系；云图中的四块墙板却只显示深色背板，原因是彩色内衬与置物细节被放到墙体外侧。必须翻转到室内侧并保留实体框、层板、便签和收纳厚度。
6. **云端旅程仍有四个确定失败点**：影院宽景和电竞宽景被首帧 readiness 假阴性拦截；影院最高排站起后直行到出口时被同排座椅提示抢占；独立同步旅程仍使用不适合云端低帧长路段的固定超时；手机首帧同样被过严判定中止。修复必须保留目标空间、WebGL、真实渲染和权威移动校验，不能通过取消断言制造假绿。
7. **维护性仍需收口**：`venueInteriors.tsx` 保留了已被新影院/电竞模块替代的整套旧实现，历史语法错误也曾发生在该区域；影院、电竞屏幕/台面尺寸还在视觉层和共享布局层双重维护。视觉坐标稳定后必须删除死实现，并把边界、门、屏幕、座位行、可行走台面和碰撞足迹集中为纯数据空间契约。

针对本次审计已立即落实两项维护性修正：删除旧 `venueInteriors.tsx` 及无布局引用的旧影院、电竞、麻将房和旧机位组件，将仍在使用的团子茶水台/灯笼迁到独立 `clubVenueProps.tsx`；新增 `shared/src/venueSpatialContracts.ts`，统一影院/电竞边界、门、主屏尺寸、影院座位列和八机位坐标，`layouts`、场馆架构与通用 `MediaScreen` registry 均消费同一数据。后续仍应在不改变稳定导入路径的前提下，把三个大型建筑文件进一步拆为 architecture、furniture/station 和 shared geometry 子模块。

在修复上述问题、重新产生全部九张证据并逐图确认前，本轮仍不得标记为完成。

### 2026-08-09：第四轮云端实拍审视（功能证据齐全，美术仍未通过）

依据提交 `a9049b0`、GitHub Actions 运行 `31312573433` 的生产构建、完整日志与九张实际截图，本轮类型检查、单元测试和生产构建全部通过；主冒烟旅程真实完成街道、团子馆、影院最高排、影院出口、电竞馆和桌游流程，单播放器/晚加入恢复、手机权威移动与九张规定尺寸 PNG 也均产出。浏览器 job 的唯一失败来自独立媒体同步脚本仍使用固定 `10s` 页面轮询，不代表场景打不开，但也不能据此宣称整体验收完成。

1. **街道路口已形成都市峡谷，但材质和前景仍过密**：桌面广角能读出高楼、媒体塔、柱网、斑马线、灯杆和多层招牌，紧凑十字路口目标成立；路面黑白图形、透明前景、线缆、信号柱和多层 HUD 仍互相切割，部分暗色金属退化为纯黑块。后续重点是减少遮挡、校准材质与接触关系，不得再靠增加霓虹字牌掩盖。
2. **影院街道近景右侧暴露空天与断边**：入口雨棚、内凹门厅、厚门框和门墩已经可读，但当前肩位镜头把建筑压到左半幅，右侧出现大面积纯蓝天空、黑色空洞和突兀终止的路沿/护栏。应修正真实玩家近门构图，让门厅居中并保留侧立面深度，而不是为截图增加假建筑。
3. **手机街景信息层级拥堵**：`390×844` 已能看到高层媒体立面，但地点栏、任务纸条、三条地点标签、聊天、目标详情、四个动作键和底部 Dock 同时占据窄屏，目标条贴边且聊天低对比。必须重新划分安全区和优先级，保留关键交互但减少互相覆盖，不能退化为卡片列表。
4. **影院屏幕和几何已修复，宽景构图仍不合格**：24m 巨幕已有原创待机内容，幕框、帷幕、低频柜、线阵音箱、六级座席、扶手和侧墙吸音构件均可识别；宽景机位过低，最近一排六个椅背遮住巨幕下半和全厅规模。应只在未落座且保持入场朝向时抬高真实玩家镜头，最高排已通过的斜视构图不得破坏。
5. **电竞馆全景成立，但八机位缺少一眼可数性**：42×34m 空间、主屏、双侧比分屏、桁架、三级赛台、两侧看台和设备席均已显示且不再黑屏；两排机位背向布置使观众广角只明显看到一排屏幕，另一排显示器背壳合并成黑块。保持 `nc-s0..7` 坐标和共享媒体逻辑，用实体背壳状态副屏、设备层次和材质分级表达完整八席，不为了截图移动业务点。
6. **团子馆温馨紧凑，但近景仍有未完成观感**：围坐沙发、筹备桌、象棋/飞行棋、柜体、墙板和木地板已形成社团活动室，风格应继续温暖而不是强行套用街道红黑；右侧仍有大面积深褐纯墙，地毯稀疏白流苏像穿插订书钉，个别座位与团子身体相交。需要补实体墙面分层、柔和织物边缘和座位净空，同时保留所有桌游与座位 ID。
7. **云端失败原因必须区分产品回归与基础设施漂移**：提交 `0f4219d` 的运行 `31322161209` 在浏览器旅程开始前即因临时安装 Playwright 触发 npm 重新解析，将 `@react-three/postprocessing ^3.0.4` 漂移到要求更高 Three.js 的 `3.0.5`，产生 `ERESOLVE`。Playwright 必须作为仓库内精确锁定的开发依赖，由 `npm ci` 安装；云端不得再临时重算项目依赖树。

本轮仍不得标记为最终完成。下一轮必须同时确认：媒体同步旅程在双 SwiftShader 客户端下使用有硬上限且有实际状态诊断的等待；锁文件安装不漂移；上述镜头、手机 HUD、团子馆和八机位可读性修改重新通过类型、构建、浏览器流程及九图人工审视。

### 2026-08-10：第五轮实现与待验收项

提交 `380c13e` 的 GitHub Actions 运行 `31322501881` 已完整通过类型检查、单元测试、生产构建、多人浏览器旅程、媒体同步、移动触控和九图文件校验。由临时安装 Playwright 引发的依赖树漂移已通过仓库内精确锁定依赖解决；双 SwiftShader 客户端下的固定十秒媒体轮询也已改为有状态诊断和硬上限的等待。因此，后续云端失败必须按新的日志与截图判断，不能继续归因于这两项已消除的基础设施问题。

针对第四轮实拍，本轮已落实但尚待新云图确认的修改包括：影院未落座出生镜头使用真实玩家相机抬高取景，并在移动时用短促阻尼退出；街道影院近景改为瞄准实体入口并收窄肩位；手机竖屏重新划分任务、地点、聊天、路线、摇杆、动作键和 Dock 安全区；电竞八个既有机位增加有厚度的背部设备壳与原创编号状态屏；团子馆拆除误建的整面深褐“角柱”，让侧墙木作与真实墙体接触，重做暖色壁灯、织物地毯包边与实例化流苏，并按家具局部坐标校正座位高度和净空。角色模型、业务 ID、媒体播放器数量和共享同步协议均未改变。

下一次云端九图必须重点人工核对：影院宽景是否越过前排椅背且不影响最高排构图；街道影院近景是否消除右侧空天、断边和黑洞；`390×844` HUD 是否不再互相覆盖且真实触控仍通过；电竞广角是否能一眼辨认八个机位；团子馆侧墙是否无悬空、地毯边缘是否读成织物、落座后团子是否不再穿入靠背。新提交的云端流程和九图未全部通过前，仍不得标记为完成。

提交 `1d047a4` 的运行 `31323510663` 再次证明依赖锁定和媒体同步修复有效：质量 job 全绿，双客户端视频的设置、seek、暂停、倍速、同一服务器时刻进度和清屏全部通过，手机端真实 Chromium touch 产生了 2.14m 服务端权威位移，E 键 keydown/keyup 链路也通过。浏览器 job 的失败来自两个更窄的独立问题，而不是世界打不开：其一，团子馆飞行棋近景结束后，出口最后一段真实步行仍写死 `8s`，日志从截图完成到失败恰好八秒，随后影院检查全部是同一空间未退出造成的级联；其二，全屏压力测试第七轮退出时，React 已保留唯一 iframe 与 `data-mark`，但低帧率 WebGL 尚未推送下一帧 CSS3D 矩阵，旧断言把临时空 transform 当成播放器重建。修复必须保留真实输入、碰撞、E 键空间切换、单 iframe 与同实例验证，不能通过传送、写入 space 或删除断言绕过。

该失败运行仍上传了七张可用实拍。确认改善的项目是：移动 HUD 各安全区已基本分离；团子馆右侧不再出现整面误建暗墙；电竞广角和近景均可由原创背部状态屏辨认完整 `P01–P08`。仍未通过的项目是：影院街面右侧仍有空天与黑色道路断层；地毯流苏虽已实例化但粗宽得像键帽；街道中心三只无支撑圆环与多层前景继续切碎构图；木材、布料、混凝土和金属在实际 SwiftShader 截图中仍显得同质；暗色电竞地面上的角色接触阴影过弱。后续实现应分别从真实相机肩位、细束实例化流苏、带支撑且减量的街道构件、有界程序化 albedo 色差和更清晰的接触阴影解决，不能以新增招牌或提高 Bloom 代替。

针对上述七图诊断，本轮已经落实但仍须由下一次云图复核的视觉修正包括：影院街面相机移到入口西侧并继续锁定真实门点；路口三只悬浮环缩减为一只并补三根实体径向支撑；地毯边缘改成更细、更密、每块地毯单 draw call 的实例化织物流苏；角色外观和几何保持不变，仅增强脚下羽化接触阴影；统一材质工厂新增十四类原创、确定性、项目内生成的 sRGB albedo 微表面，使混凝土斑驳与分缝、砖缝、金属板缝与拉丝、木纹、织物编织、沥青修补和人行道板缝拥有不同的实际光照读法。大楼实体立面按约 `3.2m` 的物理跨度重建 UV，小构件设置最小采样跨度，玻璃、屏幕与海报仍保留原始 UV；粗糙度改用独立的高灰度有界纹理，不再用深色凹凸图把混凝土和织物乘成光滑塑料。所有纹理缓存有固定上限，低规格大壳体仍可关闭微表面采样；这些结果在新一轮桌面与手机实拍完成前不能标记为通过。

提交 `4711b06` 的运行 `31325602992` 再次通过类型检查、单元测试、生产构建、媒体同步、十轮全屏同实例往返、晚加入恢复、手机真实触控与电竞馆两段旅程，且主 smoke 控制台错误为零；新增 albedo、独立 roughness 与按米 UV 没有造成 SwiftShader 首帧或截图超时。唯一直接失败仍发生在团子馆：飞行棋座位 `z=-1.58` 的服务器起立目标经桌体碰撞解算并保留两位小数后落在 `z=-1.78`，权威位移恰为 `0.20m`；旧脚本严格要求 `>0.2m`，因此在座位实际已经释放、`seatId` 已为空时仍固定等待八秒并误判失败，随后复用仍在团子馆的玩家导致影院两图级联缺失。修复后的验收必须改为“服务器座位释放 + 时间更新的权威快照已退出 Sit 动画”，并将影院作为从真实广场出生、沿共享路线进公共门的独立旅程；团子出口自身失败仍必须保持红灯，不能由旅程隔离掩盖。

该运行的七张新实拍证明材质方向有效但尚未完成：团子馆木地板已能读出木纹，地毯细流苏不再像键帽，墙漆、木作、布艺和金属的响应开始分离；街道立面也出现了按比例的拼缝和斑驳。仍需继续修正的是：影院街面入口右侧依然暴露较大的空天与黑色道路终止，入口应通过真实相机瞄点落在右侧视觉三分线；路口单环的金属支撑在暗部不够可见，需提高实体支撑读性并降低环体过曝；团子近景必须先完成权威起立再截图，避免“座位有人”与角色压住椅背/棋盘的误导。电影院两图在下一轮重新生成并人工审视前，整体仍不得标记为完成。

下一轮实现不再仅依赖镜头遮挡断边。影院东侧已从共享建筑东缘继续建立约十米宽、约二十六米高的实体服务翼，正立面和转角侧立面均由梁柱、楼层带、内凹厚窗、百叶、雨棚拉杆、排水管和屋顶设备组成；人行道、双路缘、排水格栅与沥青从可玩边界小幅重叠后延伸至不可达画外区域。该视觉收口不扩大 `CITY_BOUNDS`，也不改变电影院门、路线、碰撞与交互。影院入口镜头同时改为瞄准门点西侧，使入口落在右侧视觉三分线；手机出生镜头使用更宽的纵向视场和更长镜臂，目标是让道路与多个入口重新进入竖屏，而不是由单块广告占满画面。这两项只能由下一轮桌面近景和 `390×844` 实拍判定。

电竞馆针对“中型演播厅、低桁架、蓝色原型网格和黑紫同质”的实拍问题新增第二层空间：两侧各增加四排上层碗形看台、实体阳台底板与前缘、栏杆、顶层包厢窗和结构框，新增座椅继续并入原有实例批次；顶桁架抬高并后移到十三米顶棚以内，比赛地面改为有接缝的哑光橡胶馆地。舞台面、桌面、钢架、矿物基层、座椅织物和吸音布分别校准 albedo、粗糙度与金属度，机位显示器散热鳍、键帽、主机风口和走线架改为实例化实体细节；中性主照仅重调既有灯，未增加真实灯数量。上述修改保持八机位、座位 ID、媒体同步和共享碰撞契约不变，但仍须在新的电竞广角与舞台近景中确认上层规模、暗部材质和人物接触阴影是否真正可读。
### 2026-08-10：第六轮需求扩展（本轮云端待验收）

本轮将用户新增要求落到共享数据与场景组件，而不是只改招牌：街道布局移除静态街头 NPC/人群（在线玩家角色仍保留），电竞馆改为中轴五人连续选手席、后排三席的业务坐标契约，并在顶棚下增加四块有实体外壳和吊装线缆的赛事大屏；看台继续以东西侧、南后场和上层碗形结构围合中央比赛舞台。影院保留 34×30m 巨幕厅、六级坡台和真实座椅业务，后续截图必须能同时读到幕框、阶梯和厅内纵深。团子馆的点歌机沿用现有服务端 `music_set`/同步合成器与可点选曲目；飞行棋新增服务端权威四人棋局、加入/掷骰/移动/结束回合/离开协议，视觉上改为地摊低桌、织物垫与四个地垫坐垫，原有座位交互距离不变，且 3D 棋盘会根据权威棋局显示棋子沿 52 格移动及终点堆叠。街景新增少量受控的动态色带、速度线和高位彩色线缆，不能重新堆叠无支撑平面或遮挡入口。该轮代码已完成静态接线，尚未通过新的云端九图与完整游戏旅程，不能据此标记最终验收。

### 2026-08-10：第七轮实体连接与街景动态层

- 电竞馆首发五席新增共享实体选手台：贯穿底座、后部线缆槽、五个独立服务分区和可切换的队伍色标。`nc-s0..4` 的交互点、后三个备用席和既有座位业务不变；新增结构不占用主通道，也不新增真实灯光。
- 团子馆点歌机在保留 `music_set` 同步协议与原曲目列表的基础上重做实体外壳、唱片机构、扬声器、均衡器和当前曲目屏；播放状态、曲名与 BPM 会在场景本体中反馈，关闭动效时保持静态可读。
- 街景新增两组短距离动态光轨。光轨由底座、侧向支撑和厚度明确的色带构成，只放在道路两侧，避免遮挡影院、观战馆和媒体塔入口；动画幅度受控，并保留 `prefers-reduced-motion` 由上层动效开关统一裁剪的路径。
- 本轮仍只做静态检查并推送云端，不能把本地未运行测试当作通过依据。下一次云端九图重点检查：五席是否连成一个舞台前景、光轨是否抢过地标、移动端是否出现遮挡，以及所有新增实体是否保持接地。

### 2026-08-10：第八轮绚丽度与空间气势强化

- 街景的高饱和色不再只存在于招牌和高位光轨：十字路口四条道路的边缘增加分段式实体导光构件，由金属承托层和青、粉、黄三色灯芯构成，并主动断开斑马线、场馆入口和主要通路。它们负责把色彩延伸到路面透视中，而不是增加新的悬浮广告牌或实时灯源。
- 电影院在 34×30m 巨幕厅原有六级坡台、24m 银幕和实体座椅之外，新增三重收分的顶棚冠架、吊杆连接、中央吸音穹顶与暖色导光内圈。该层只强化厅堂尺度和酒红/金属/木作层次，不改变座位、坡台、屏幕、媒体同步和碰撞契约。
- 电竞观战馆新增环场赛事飘带：两侧上下层看台前缘、南后场看台和主赛台北侧形成连续但有断点的青紫粉赛事色层，全部附着在实体深色金属壳体上；原有五人连坐主选手席、三席备用区、四块吊装屏、围合看台和真实灯光预算均保持不变。
- 团子轰趴馆新增木作圆形藻井、奶油色吸音内盘、织物色内圈、实体连接件与八盏小型吊灯，形成温暖明确的房间中心；不增加真实灯光数量，不覆盖点歌机、地摊飞行棋、象棋、筹备桌和座位通路。
- 本轮没有新增依赖、外部模型、版权素材或角色改动，也没有在本地运行测试、构建和浏览器。新的空间冠部、道路色带和环场赛事层必须通过下一次云端桌面/手机截图确认亮度、遮挡、接地与 SwiftShader 性能，未看到新证据前不能宣称最终美术验收完成。

### 2026-08-10：第九轮中景豪华层与建筑色彩连续性

- 电影院两侧高墙新增带厚底板、织物前缘、金属立柱、斜撑和暖色导光边的高位廊台，使六级座席之上仍有第二层空间尺度；廊台位于不可达高位，不改变地面碰撞、过道、座位和巨幕交互。
- 电竞馆东西看台中央疏散口新增实体门洞：混凝土边柱、深色钢门楣、内侧金属收边和分区色导光共同围出净通口。中央轴线和真实玩家通路保持开放，五人主选手席、四块吊屏、三席备用区及共享碰撞数据均未移动。
- 团子馆北墙展示区增加六段软包织物帷幔、木质压条、暖色节点与两侧束带，和上一轮圆形藻井形成上下呼应；帷幔只占墙面高位，不覆盖点歌机、地摊飞行棋、家具或交互提示。
- 街景影院东侧服务翼的三层楼层结构增加连续实体色带，同时提高深色玻璃、暖橱窗、影院与电竞结构色在 SwiftShader 暗部中的基准可读性；色彩仍附着于梁、楼层带和厚窗体，没有增加悬浮平面、实时灯光或 Bloom 依赖。
- 本轮继续保持角色模型、业务 ID、多人同步、媒体播放和房间协议不变；只允许云端运行验证，本地仍仅做源代码与差异静态复核。

## 2026-08-10：P5R 抽象美术原则对照审视与两小时持续优化基线

本阶段只借鉴参考画面中可抽象、不可版权垄断的视觉原则：强烈明暗剪影、非对称主次关系、建筑透视线汇聚、局部高饱和色群、前中后景快速分层、都市设施的人体尺度，以及图形信息和真实空间互相咬合的节奏。禁止复制 Persona 5 Royal 的角色、字体、标志、地图、建筑、贴图、UI 或画面布局。

### 当前差距

1. **街道色彩过于平均**：现有青、粉、黄灯带已经增加亮度，但沿道路和立面近似均匀分布，缺少参考画面中“一个主屏/入口承担焦点、其余色彩沿透视线服务焦点”的组织。路口的电线、交通设施、地点标签和媒体塔仍可能同时争夺视线；静态人群删除后，还需要用橱窗纵深、路面反射、设备和近景结构补足生活密度，而不是把空位重新填满招牌。
2. **街道黑白红层级不够明确**：深色楼体、彩色灯和浅色墙面多是并列关系，缺少大块暗剪影托住少量红色行动线的力度。后续红色只集中到当前路线、影院和任务方向，青紫用于远处都市层，黄色用于人行尺度和入口，不允许三色平均铺满。
3. **电影院阶梯在宽景中不够醒目**：巨幕、实体座椅、侧墙和顶棚结构已经成立，但六级坡台主要依靠暗地毯明暗表达，低帧云端广角容易把多排座席压成一个黑色斜面。需要按共享坡台高度增加可见前缘、材质分区和受控暖/冷节奏；巨幕仍是唯一第一焦点，不能由顶棚装饰抢占。
4. **电竞馆过度镜像**：中央五席、四块吊屏和环形看台符合业务要求，但东西两侧的构件、灯带和颜色接近完全对称，画面容易像演播厅或展示模型。需要增加一处真实的非对称广播/摄影设施、不同侧的材质权重和斜向结构，同时保持比赛台、八席业务点和疏散通道不变。
5. **电竞设备近景仍偏黑**：主机、桌体、座椅和钢架虽然已区分材质，实际截图中仍容易合并成黑紫块。后续优先增加可观察的金属边、织物面、散热结构和中性反射面，不通过提高 Bloom 或增加实时灯数量解决。
6. **团子馆装饰节奏偏规则**：房间已温暖且功能完整，但吊灯、帷幔、墙板和家具容易形成平均网格。后续让点歌机/地摊飞行棋/围坐区构成三角焦点，软装使用大小、深浅和材料差异形成生活化簇群；不得硬套街景红黑配色，也不得堵塞真实通路。
7. **云端验收仍是唯一运行依据**：每轮提交只允许本地静态差异检查，类型、构建、浏览器、九图和多人旅程全部由 GitHub Actions 执行。任何新增装饰如果在真实玩家镜头中不可识别、遮住入口、造成黑屏或明显降低 SwiftShader 稳定性，下一轮必须删除或重做，不能以代码存在作为完成依据。
